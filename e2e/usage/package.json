{
  "name": "test-consumer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:package": "bash test-package.sh",
    "test:docker": "docker compose up --build --abort-on-container-exit"
  },
  "dependencies": {
    "@nimir/dot-path": "file:../.."
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.14"
  }
}
